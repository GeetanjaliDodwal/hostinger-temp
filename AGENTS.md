# AGENTS.md

## Cursor Cloud specific instructions

This is currently an empty placeholder repository containing only a `README.md` with no application code, dependencies, or services.

- **No build system, package manager, or language runtime** is configured.
- **No tests, lint, or build commands** exist to run.
- **No services** need to be started.

When application code is added to this repository, update this section with:
- How to install dependencies and which package manager to use.
- How to run lint, tests, and the dev server.
- Any non-obvious setup caveats discovered during development.
